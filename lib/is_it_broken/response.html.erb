<!DOCTYPE html>
<html>
  <head>
    <title><%= IsItBroken.application_name %> Status</title>
    <style type="text/css">
      body {
        font-size: 13px;
        font-family: sans-serif;
      }
      h1 {
        font-size: 18px;
        font-weight: bold;
      }
      table {
        width: 100%;
        border-size: 0;
        padding: 0;
        margin: 0;
        border-spacing: 0;
      }
      td, th {
        text-align: left;
        font-size: 13px;
        font-family: sans-serif;
        margin: 0;
        padding: 10px;
        border-bottom: 1px solid gray;
      }
      th {
        font-weight: bold;
      }
      .success {
        color: green;
      }
      .failure {
        color: red;
      }
      .warning {
        color: orange;
      }
    </style>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="noindex, nofollow, nosnippet">
  </head>
  <body>
    <h1><%= IsItBroken.application_name %> Status</h1>
    <p>
      <div>Timestamp: <%= timestamp.httpdate %></div>
      <div>Elapsed Time: <%= elapsed_time_ms %>ms</div>
    </p>
    <table>
      <thead>
        <tr>
          <th>Status</th>
          <th>Name</th>
          <th>Message</th>
      </thead>
      <tbody>
        <% results.each do |result| %>
          <% result.assertions.each do |assertion| %>
            <tr class="<%=assertion.status%>">
              <td><%= assertion.status_label %></td>
              <td><%= result.name %></td>
              <td><%= assertion.message %></td>
            </tr>
          <% end %>
        <% end %>
      </tbody>
    </table>
  </body>
</html>
