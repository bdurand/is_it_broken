# frozen_string_literal: true

require "spec_helper"

describe IsItBroken::Check::File do
  it "can check if a file exists"

  it "can check if a file is readable by the current user"

  it "can check if a file is writable by the current user"

  it "can check if a file is a plain file"

  it "can check if a file is a directory"

  it "can check if a file is a socket"

  it "can check if a file is a pipe"

  it "can alias the file path"
end
